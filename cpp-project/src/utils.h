#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <string>

// Function to read input from the user
double readInput(const std::string& prompt);

// Function to display the result to the user
void displayResult(const std::string& message, double result);

#endif // UTILS_H